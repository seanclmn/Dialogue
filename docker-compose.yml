services:
  db:
    image: mysql
    environment: 
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_DATABASE=test
    ports:
      - 3306:3306
    restart: always
    # volumes: 
    #   - /Users/seanclmn/Projects/chat/db:/var/lib/mysql